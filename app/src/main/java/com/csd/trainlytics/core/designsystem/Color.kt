package com.csd.trainlytics.core.designsystem

import androidx.compose.ui.graphics.Color

// ── Surface Hierarchy ──────────────────────────────────────────────────────
val Surface = Color(0xFF0E0E0E)                 // Global background (Obsidian)
val SurfaceContainerLowest = Color(0xFF0B0B0B)
val SurfaceContainerLow = Color(0xFF131313)     // Large content areas / sections
val SurfaceContainer = Color(0xFF1A1A1A)        // Primary cards
val SurfaceContainerHigh = Color(0xFF20201F)    // Elevated cards
val SurfaceContainerHighest = Color(0xFF262626) // Chart backgrounds / inputs

// ── Primary – Action Green ─────────────────────────────────────────────────
val Primary = Color(0xFF3FFF8B)
val PrimaryContainer = Color(0xFF13EA79)
val OnPrimary = Color(0xFF00200F)
val OnPrimaryContainer = Color(0xFF002115)

// ── On-Surface ─────────────────────────────────────────────────────────────
val OnSurface = Color(0xFFE6E6E6)
val OnSurfaceVariant = Color(0xFF9B9B9B)

// ── Accent colors ──────────────────────────────────────────────────────────
val Tertiary = Color(0xFF6E9BFF)       // Hydration / neutral metrics
val TertiaryContainer = Color(0xFF1A3A7A)
val Error = Color(0xFFFF716C)          // Deficits
val ErrorContainer = Color(0xFF4A1010)

// ── Outline ────────────────────────────────────────────────────────────────
val OutlineVariant = Color(0xFF484847)  // Use at 20% opacity only

// ── Nutrition macro colors ──────────────────────────────────────────────────
val MacroProtein = Color(0xFF3FFF8B)   // Primary green
val MacroCarbs = Color(0xFF6E9BFF)     // Tertiary blue
val MacroFat = Color(0xFFFFB347)       // Warm amber
val MacroCalories = Color(0xFFFF716C)  // Error coral
